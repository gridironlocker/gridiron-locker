# app — APP module

Spec §2 logical module: see radar/README.md and radar/lib/ for implementation.
This folder holds the app code / future expansion for app; core logic is in radar/lib/ and radar/lib/agents/ so the architecture is modular and expandable without rewriting.
